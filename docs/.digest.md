# Project Digest: HarnessKit

## OVERVIEW
Clean Architecture modular em TypeScript/Node.js para orquestração autônoma de TDD com interfaces CLI e Web local (`sdk-web`). Camadas divididas em Domain, Application, Presentation e Adapters/Infrastructure com isolamento hermético de processos.

## CONSTRAINTS & PATTERNS
- REQUIRED: Herança de `AbstractCliRunner` com `writePromptToStdin = true` para streaming de prompts extensos.
- REQUIRED: Servidor web local vinculado estritamente a `127.0.0.1` prevenindo exposição em interfaces de rede.
- REQUIRED: Sanitização obrigatória de variáveis sensíveis de ambiente via `filterSensitiveEnv`.
- REQUIRED: Mapeamento 1:1 estrito entre CycleId e SessionId em execuções paralelas.
- REQUIRED: Isolamento concorrente em disco via Git Worktrees (`WorktreeIsolationProvider`).
- FORBIDDEN: Propagação de tokens, API keys ou senhas em processos filhos ou payloads de telemetria.
- FORBIDDEN: Terminação de processo único sem encerramento da árvore de processos (`taskkill /t` / `SIGKILL`).

## TESTING
- Framework: Vitest ^4.1.10
- Commands: `npm.cmd test` (SDK), `npm.cmd --prefix sdk-web test` (Web), `npm.cmd run test:e2e` (E2E)
- Gated Live Tests: `env AGY_INTEGRATION_TEST=true npm.cmd test`
- Minimum Coverage: 85% global (90% domain/core, 85% app/presentation, 80% infra)

## DOCUMENTATION INDEX
- `docs/adr/ARCHITECTURE.md`: Arquitetura do projeto, camadas e padrões de execução de agentes.
- `docs/adr/TESTS.md`: Protocolo de testes, comandos e estratégias de mock e integração.
- `docs/feature/session-cycle-management.md`: Gestão e persistência atômica de ciclos e sessões.
- `docs/feature/multi-cycle-verification.md`: Suíte E2E automatizada de verificação multi-ciclos.
- `docs/feature/parallel-cycle-management.md`: Gestão de ciclos paralelos concorrentes com sessões próprias 1:1 e Git Worktrees.
- Required read `docs/.graph.json` for the complete document list, tags, and relations.

## ROUTING
Use an exact supplied path directly; otherwise use `.graph.json` to select one feature, extract only its top graph block, then read routed source files. Read document prose only when the task requires design context.

## LAST UPDATED
2026-09-02